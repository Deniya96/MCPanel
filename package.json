{
  "name": "mcpanel-root",
  "private": true,
  "scripts": {
    "start": "concurrently -n backend,frontend \"npm run start --prefix backend\" \"npm run start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
